const listNumber = [1, 2, 3, 4, 5];

// splice: dùng để xóa
// arr.splice(index, deleteCount)
// console.log(listNumber);
// listNumber.splice(0, 1);
// console.log(listNumber);

// splice: dùng để thêm
// arr.splice(index, 0, element1, element2,... elementN)
// console.log(listNumber);
// listNumber.splice(3, 0, 'ba', 'abc', 'xyz');
// console.log(listNumber);
